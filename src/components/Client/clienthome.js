import React from "react";
import ClientAppBar from "../Nav/client";

const ClientHome = () => {
    return (
        <div className="ClientHome"> 
            <ClientAppBar />
            Client Homepage. Include what is needed for home page </div>
    );
};

export default ClientHome;